---
description: "Fix the dotnet basic-fail fixture so it has a genuinely uncovered function, remove the four expected_status workarounds from cli_metrics.rs, and extract inline #[cfg(test)] blocks from istanbul_json.rs and gate.rs that have no private-API access requirement. Read when implementing, reviewing, or continuing this cleanup."
---

# Fix Dotnet Fixture and Extract Misplaced Inline Tests

## Goal
- `tests/fixtures/dotnet/basic-fail` has an uncovered function so function-threshold rules produce a real failure
- All four `if fixture.language == "dotnet" { 0 } else { 1 }` workarounds in `tests/cli_metrics.rs` are removed; every test in the function-threshold matrix asserts uniformly
- The `#[cfg(test)]` block in `src/coverage/istanbul_json.rs` is extracted to `tests/coverage_parse.rs`
- The `#[cfg(test)]` block in `src/gate.rs` is extracted to a new `tests/gate.rs`

## Scope
- In: dotnet basic-fail fixture source + coverage regen, cli_metrics.rs workaround removal, istanbul_json.rs test extraction, gate.rs test extraction
- Out: llvm_json.rs and coverlet_json.rs inline tests (those call private functions and are correctly placed); named-function work on `feat/namedFunctions` (stashed separately)

## Relevant Areas
- `tests/fixtures/dotnet/basic-fail/repo/src/CovgateDemo/MathOps.cs` — fixture source; needs a second uncovered method
- `tests/fixtures/dotnet/basic-fail/overlay/src/CovgateDemo/MathOps.cs` — overlay source; needs the same second method
- `tests/fixtures/dotnet/basic-fail/coverage.json` — regenerated by xtask; must not be hand-edited
- `tests/cli_metrics.rs` — contains four `expected_status` workarounds to remove (lines 360, 421, 456, 516 in current working tree; lines 360 and 421 in HEAD)
- `src/coverage/istanbul_json.rs` — `#[cfg(test)]` block (~lines 260–641) calls only `pub(crate) parse_with_repo_root`; no private access
- `src/gate.rs` — `#[cfg(test)]` block (~lines 48–196) calls only `pub fn evaluate`; no private access
- `tests/coverage_parse.rs` — destination for istanbul tests; follow existing structure
- `tests/gate.rs` — new file; destination for gate tests
- `xtask/src/main.rs` — `regen-fixture-coverage dotnet/basic-fail` is the regeneration command

## Open Questions
- None yet

## Steps

### Part 1 — Fix dotnet fixture

- [ ] In `tests/fixtures/dotnet/basic-fail/repo/src/CovgateDemo/MathOps.cs`, add a second public static method `Subtract(int a, int b)` that is never called by the test suite
- [ ] Apply the same addition to `tests/fixtures/dotnet/basic-fail/overlay/src/CovgateDemo/MathOps.cs`
- [ ] Run `cargo xtask regen-fixture-coverage dotnet/basic-fail` to regenerate `coverage.json` with two functions, one covered (`Add`) and one uncovered (`Subtract`)
- [ ] Verify the regenerated `coverage.json` contains `Subtract` with all line hits equal to `0`
- [ ] Remove the four `if fixture.language == "dotnet" { 0 } else { 1 }` blocks from `tests/cli_metrics.rs`; replace each with `assert_eq!(output.status.code(), Some(1), ...)` and unconditional FAIL assertions

### Part 2 — Extract istanbul_json.rs inline tests

- [ ] Move all test functions from the `#[cfg(test)]` block in `src/coverage/istanbul_json.rs` into `tests/coverage_parse.rs`
- [ ] Update imports in the moved tests: replace `use super::parse_with_repo_root` with `use covgate::coverage`; call `coverage::load_from_path` or `coverage::parse_with_repo_root` as appropriate following the pattern already in `tests/coverage_parse.rs`
- [ ] Delete the entire `#[cfg(test)]` block from `src/coverage/istanbul_json.rs`
- [ ] Confirm `src/coverage/istanbul_json.rs` compiles and inline tests are gone

### Part 3 — Extract gate.rs inline tests

- [ ] Create `tests/gate.rs`
- [ ] Move all test functions from the `#[cfg(test)]` block in `src/gate.rs` to `tests/gate.rs`
- [ ] Update imports: `use covgate::gate::evaluate` (or however `evaluate` is re-exported); `use covgate::model::{ComputedMetric, GateRule, MetricKind}`
- [ ] Delete the `#[cfg(test)]` block from `src/gate.rs`
- [ ] Confirm `src/gate.rs` compiles and the test file runs

### Part 4 — Validate

- [ ] `cargo test --test cli_metrics` — all function-threshold tests pass with uniform assertions
- [ ] `cargo test --test coverage_parse` — all istanbul tests pass in their new location
- [ ] `cargo test --test gate` — all gate tests pass in their new location
- [ ] `cargo xtask validate` — full suite passes

## Validation
- `cargo test --test cli_metrics` — no dotnet special-cases; function-threshold matrix passes for all fixtures
- `cargo test --test coverage_parse` — istanbul tests present and green
- `cargo test --test gate` — gate tests present and green
- `cargo xtask validate` — full format, lint, test, coverage, and audit sweep passes
- Manual: confirm `tests/fixtures/dotnet/basic-fail/coverage.json` has a method with all-zero line hits

## Discoveries
- None yet

## Review
- [ ] None yet