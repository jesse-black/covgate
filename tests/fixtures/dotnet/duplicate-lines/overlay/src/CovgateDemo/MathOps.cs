namespace CovgateDemo;

public static class MathOps
{
    public static int Add(int value)
    {
        int Duplicate(int input) => input + 1;
        return value < 0 ? value : Duplicate(value);
    }
}
